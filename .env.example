# Mia 云中继环境变量示例
# ----------------------------------------------------------------------------
# 使用方法：
#   1) 复制为 .env：   cp .env.example .env
#   2) 填入真实值
#   3) docker compose up -d
#
# 真实的 .env 已被 .gitignore 排除，请不要提交到仓库。
# ----------------------------------------------------------------------------

# 合法 Bearer token 白名单（逗号分隔）。
# 每个 token 对应“一个账号/一对手机+Agent”，同一 token 可被手机与 Agent 并发连接一次。
# 生成推荐：openssl rand -base64 32
# 示例：MIA_AUTH_TOKENS=3h7Jm0Zx...==,4aN2qP9s...==
MIA_AUTH_TOKENS=

# 对外提供服务的域名。Caddy 会用该域名向 Let's Encrypt 申请 HTTPS 证书。
# 该域名的 DNS A/AAAA 记录必须指向部署本中继的主机，且 80/443 已对公网开放。
# 示例：MIA_DOMAIN=mia.example.com
MIA_DOMAIN=
